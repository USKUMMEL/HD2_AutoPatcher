# App icon

Place the Windows icon file for HD2 Patch Fixer here with this exact name:

```text
icon.ico
```

Then run `build.bat`. The icon is used for both `dist\HD2PatchFixer.exe` and the app window.
